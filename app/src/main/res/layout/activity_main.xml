<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FAF0E6"
    android:padding="16dp">

    <TextView
        android:id="@+id/titleText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Coffee Menu"
        android:textSize="28sp"
        android:textStyle="bold"
        android:layout_centerHorizontal="true"
        android:paddingBottom="10dp"/>

    <TextView
        android:id="@+id/cartTotalText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Cart Total: $0.00"
        android:textSize="20sp"
        android:textStyle="bold"
        android:layout_below="@id/titleText"
        android:layout_marginTop="12dp"/>

    <TextView
        android:id="@+id/pickupInfoText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Pickup only at 123 Coffee Lane, Hayward CA"
        android:textSize="16sp"
        android:layout_below="@id/cartTotalText"
        android:layout_marginTop="6dp"
        android:textColor="#555555"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_below="@id/pickupInfoText"
        android:layout_above="@id/buttonCheckout"
        android:layout_marginTop="12dp"/>

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/buttonCheckout"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:text="Checkout"
        android:textColor="#FFFFFF"
        android:background="@drawable/button_light_brown"
        android:layout_alignParentBottom="true"
        android:layout_marginTop="8dp"/>
</RelativeLayout>
